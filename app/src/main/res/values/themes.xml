<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SimpleNote" parent="Base.Theme.SimpleNote" />
    <style name="ExtendedButtonStyle" parent="Widget.Material3.FloatingActionButton.Primary">
        <item name="backgroundTint">@color/black</item>
        <item name="colorContainer">@color/black</item>
        <item name="cornerRadius">@dimen/_30sdp</item>
        <item name="iconTint">@color/white</item>
    </style>
</resources>